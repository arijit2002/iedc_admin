import React from 'react';
import {logout} from '../LoginComponent/Login';

export const Dashboard = () => {
  return (
    <div>
      <button onClickCapture={this.logout}>Logout user</button>
      </div>

  )
}
